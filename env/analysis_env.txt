# Harness + analysis env. Mirror of the pyproject deps for users who prefer a flat
# requirements file over `pip install -e .`. Prefer `pip install -e ".[embeddings,dev]"`.
openai>=1.30
datasets>=2.18
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
backoff>=2.2
pyarrow>=14.0
transformers>=4.51
sentence-transformers>=2.6
pytest>=7.4
